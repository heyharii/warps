/** PostCSS — Tailwind v4 utilities for bklit chart components.
 *  SCSS is compiled separately by Next's sass pipeline; this runs after. */
const config = {
  plugins: {
    '@tailwindcss/postcss': {},
  },
};

export default config;
